export enum PageEnum {
  // 登录
  BASE_LOGIN = '/login',
  BASE_LOGIN_NAME = 'Login',
  //重定向
  REDIRECT = '/redirect',
  REDIRECT_NAME = 'Redirect',
  // 首页（前台首页）
  BASE_HOME = '/',
  //后台首页（管理后台）
  BASE_ADMIN_HOME = '/admin',
  //首页跳转默认路由（后台仪表盘）
  BASE_HOME_REDIRECT = '/admin/dashboard',
  // 错误
  ERROR_PAGE_NAME = 'ErrorPage',
}
